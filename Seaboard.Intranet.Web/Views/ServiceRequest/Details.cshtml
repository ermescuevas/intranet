@{
    Seaboard.Intranet.Domain.ViewModels.PurchaseRequestAdditionalInfoViewModel info = ViewBag.AdditionalInformation;
}

<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
    <h4 class="modal-title" id="myModalLabel">Detalles de requisicion</h4>
</div>

<div class="modal-body">
    <div class="row">
        <div class="col-lg-12">
            <div class="tabs-container">
                <ul class="nav nav-tabs">
                    <li class="active"><a data-toggle="tab" href="#tab-1"> Detalles</a></li>
                    <li class=""><a data-toggle="tab" href="#tab-2"> Inf. add.</a></li>
                    <li class=""><a data-toggle="tab" href="#tab-3"> Inf. seg.</a></li>
                    <li class=""><a data-toggle="tab" href="#tab-4"> Analisis</a></li>
                    <li class=""><a data-toggle="tab" href="#tab-5"> Ord. compras</a></li>
                </ul>
                <div class="tab-content">
                    <div id="tab-1" class="tab-pane active">
                        <div class="panel-body">
                            <table class="table table-hover">
                                <thead>
                                    <tr>
                                        <th>Id. de articulo</th>
                                        <th>Descripción</th>
                                        <th>Cantidad</th>
                                        <th>Estado</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @if (ViewBag.RequestDetail != null)
                                    {
                                        foreach (Seaboard.Intranet.Domain.ViewModels.PurchaseRequestLineViewModel item in ViewBag.RequestDetail)
                                        {
                                            <tr>
                                                <td>@item.ItemId.Trim()</td>
                                                <td>@item.ItemDesc.Trim()</td>
                                                <td>@item.Quantity.ToString("N2")</td>
                                                <td>@item.Status.Trim()</td>
                                            </tr>
                                        }
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>

                    <div id="tab-2" class="tab-pane">
                        <div class="panel-body">
                            <h4>Informacion adicional</h4>
                            @if (info != null)
                            {
                                <dl class="dl-horizontal">
                                    <dt>@Html.DisplayName("Descripción")</dt>
                                    <dd>@info.Description.Trim()</dd>
                                    <dt>@Html.DisplayName("Fecha requerida")</dt>
                                    <dd>@info.RequiredDate.ToString("dd/MM/yyyy")</dd>
                                    <dt>@Html.DisplayName("Fecha documento")</dt>
                                    <dd>@info.DocumentDate.ToString("dd/MM/yyyy")</dd>
                                    <dt>@Html.DisplayName("Departamento")</dt>
                                    <dd>@info.Department.Trim()</dd>
                                    <dt>@Html.DisplayName("Solicitante")</dt>
                                    <dd>@info.Requester.Trim()</dd>
                                    <dt>@Html.DisplayName("Aprobador")</dt>
                                    <dd>@info.Aprover.Trim()</dd>
                                </dl>
                            }
                        </div>
                    </div>

                    <div id="tab-3" class="tab-pane">
                        <div class="panel-body">
                            <h4>Inf. seguimiento</h4>
                            @if (info != null)
                            {
                                <dl class="dl-horizontal">
                                    <dt>@Html.DisplayName("Prep Sol de cotiz")</dt>
                                    <dd>@info.UserQuote.Trim()</dd>
                                    <dt>@Html.DisplayName("Prep Pre-Analisis")</dt>
                                    <dd>@info.UserPreAnalysis.Trim()</dd>
                                    <dt>@Html.DisplayName("Prep. Analisis")</dt>
                                    <dd>@info.UserAnalysis.Trim()</dd>
                                    <dt>@Html.DisplayName("Fecha ini sol de Cotiz")</dt>
                                    <dd>@info.QuoteDate.ToString("dd/MM/yyyy")</dd>
                                    <dt>@Html.DisplayName("Fecha ini Pre-Analisis")</dt>
                                    <dd>@info.PreAnalysisDate.ToString("dd/MM/yyyy")</dd>
                                    <dt>@Html.DisplayName("Fecha ini Analisis")</dt>
                                    <dd>@info.AnalysisDate.ToString("dd/MM/yyyy")</dd>
                                </dl>
                            }
                        </div>
                    </div>

                    <div id="tab-4" class="tab-pane">
                        <div class="panel-body">
                            <h4>Analisis</h4>
                            <table class="table table-hover">
                                <thead>
                                    <tr>
                                        <th>Id. de analisis</th>
                                        <th>Descripción</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @if (ViewBag.AnalysisDetail != null)
                                    {
                                        foreach (Seaboard.Intranet.Domain.ViewModels.GenericViewModel item in ViewBag.AnalysisDetail)
                                        {
                                            <tr>
                                                <td>@item.ViewDataId.Trim()</td>
                                                <td>@item.ViewDataDescription.Trim()</td>
                                            </tr>
                                        }
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>

                    <div id="tab-5" class="tab-pane">
                        <div class="panel-body">
                            <h4>Ord. de compras</h4>
                            <table class="table table-hover">
                                <thead>
                                    <tr>
                                        <th>Orden de compra</th>
                                        <th>Id. de analisis</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @if (ViewBag.PurchaseOrderDetail != null)
                                    {
                                        foreach (Seaboard.Intranet.Domain.ViewModels.GenericViewModel item in ViewBag.PurchaseOrderDetail)
                                        {
                                            <tr>
                                                <td>@item.ViewDataId.Trim()</td>
                                                <td>@item.ViewDataDescription.Trim()</td>
                                            </tr>
                                        }
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="modal-footer">
    <button type="button" class="btn btn-default" data-dismiss="modal" aria-hidden="true">Cerrar</button>
</div>