@model Seaboard.Intranet.Domain.ViewModels.Lookup
@{
    ViewBag.Title = "Asignación de usuarios";
}

<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-lg-10">
        <h2>Asignación de usuarios</h2>
        <ol class="breadcrumb">
            <li>
                <a href="@Url.Action("Index", "Home")">Inicio</a>
            </li>
            <li class="active">
                <strong>Asignación de usuarios</strong>
            </li>
        </ol>
    </div>
</div>

<div class="wrapper wrapper-content animated fadeInRight ecommerce">
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox float-e-margins">
                <div class="ibox-title">
                    <h3>Asignación de usuarios</h3>
                </div>
                <div class="ibox-content">
                    <div class="form-group">
                        <div class="col-sm-12 col-sm-offset-1">
                            <button class="btn btn-primary" type="button" onclick="guardar();"><i class="fa fa-save"></i> Guardar</button>
                            <button class="btn btn-white" type="button" onclick="limpiarCampos();"><i class="fa fa-eraser"></i> Borrar</button>
                            <br />
                        </div>
                    </div>
                    <br />
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-lg-12">
            <div class="tabs-container">
                <ul class="nav nav-tabs">
                    <li class="active"><a data-toggle="tab" href="#tab-1"> Datos Basicos</a></li>
                </ul>
                <div class="tab-content">
                    <div id="tab-1" class="tab-pane active">
                        <div class="panel-body">
                            <div class="row">
                                <fieldset class="form-horizontal">
                                    <div class="row">
                                        <div class="col-md-4">
                                            <div class="form-group">
                                                <label class="col-sm-4 control-label">Id de departamento:</label>
                                                <div class="col-sm-8 input-group">
                                                    @Html.TextBoxFor(model => model.Id, new { @class = "form-control", @readonly = "" })
                                                    <span class="input-group-btn">
                                                        <button id="lookupDepartamento" type="button" class="btn btn-primary">
                                                            <i class="fa fa-search"></i>
                                                        </button>
                                                    </span>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row">
                                        <div class="col-md-4">
                                            <div class="form-group">
                                                <label class="col-sm-4 control-label">Descripción:</label>
                                                <div class="col-sm-8 input-group">
                                                    @Html.TextBoxFor(m => m.Descripción, new { @class = "form-control", @readonly = "" })
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                    <br />
                                    <div class="hr-line-dashed"></div>
                                    <br />
                                    <div class="row">
                                        <div class="col-md-1">
                                        </div>
                                        <div class="col-md-8 text-center">
                                            <div class="table-responsive" id="userRows">


                                            </div>
                                        </div>
                                    </div>
                                </fieldset>

                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Styles {
    <link href="@Url.Content("~/Content/plugins/lookupbox/lookupbox.css")" rel="stylesheet" type="text/css" />
    @Styles.Render("~/Content/plugins/dataTables/dataTablesStyles")
    @Styles.Render("~/plugins/jasnyBootstrapStyles")
    @Styles.Render("~/plugins/dataPickerStyles")
    @Styles.Render("~/plugins/toastrStyles")
}

@section Scripts {
    @Scripts.Render("~/plugins/dataTables")
    @Scripts.Render("~/Scripts/plugins/jquery-ui/jquery-ui.min.js")
    @Scripts.Render("~/plugins/dataPicker")
    @Scripts.Render("~/plugins/jasnyBootstrap")
    @Scripts.Render("~/plugins/toastr")
    @Scripts.Render("~/Scripts/plugins/lookupbox/jquery.lookupbox.js")


    <script type="text/javascript">
        var userItems = [];

        $("#lookupDepartamento").lookupbox({
            title: 'Busqueda: ',
            url: '@Url.Action("ListLookup", "Home")?tipo=24&consulta=',
            imgLoader: '<img src="~/Images/ajaxloader.gif">',
            width: 800,
            height: 1000,
            onItemSelected: function (data) {
                $('input[name=Id]').val(data.Id);
                $('input[name=Descripción]').val(data.Descripción);
                buscarLineas()
            },
            tableHeader: ['Id. de departamento', 'Descripción'],
            hiddenFields: ['DataExtended']
        });

        function limpiarCampos() {
            $('#Id').val('');
            $('#Descripción').val('');
            $('#userRows').html('');
        }

        function buscarLineas() {
            $.ajax({
                url: '@Url.Action("GetUserDepartments", "Logistic")?DepartmentId=' + $('#Id').val().trim(),
                type: "POST",
                data: "",
                dataType: "JSON",
                cache: false,
                contentType: "application/json",
                success: function (result) {
                    userItems = result.status;
                    console.log(result.status);
                    fillUsersTable();
                }
            });
        }

        function fillUsersTable() {
            if (userItems.length > 0) {
                var $table = $('<table class="table table-stripped table-bordered" id="informationTable" />');
                $table.append('<thead><tr><th>Id. de usuario</th><th>Nombre</th><th style="width:3%" class="text-center">#</th></tr></thead>');
                var $tbody = $('<tbody/>');
                $.each(userItems, function (i, val) {
                    var $row = $('<tr/>');
                    var $check = $('<input type="checkbox" class="form-control" name="check">');
                    $check.prop('checked', val.Estado);
                    $row.append($('<td/>').html(val.Id));
                    $row.append($('<td/>').html(val.Descripción));
                    $row.append($('<td/>').html($check));
                    $tbody.append($row);
                });

                $table.append($tbody);
                $('#userRows').html($table);


            }
            else {
                $('#userRows').html('');
            }
        }

        function guardar() {
            var isAllValid = true;
            var lines = [];
            if ($('#Id').val().trim().length == 0) {
                toastr.error('Debe de especificar un id de departamento', 'Campos requeridos')
                isAllValid = false;


            }

            if (isAllValid) {
                $('#informationTable > tbody > tr').each(function () {
                    var row = $(this);
                    if (row.find('td:eq(2) input').is(':checked')) {
                        lines.push({
                            Id: $('#Id').val().trim(),
                            Descripción: row.find('td:eq(0)').html().trim(),
                            Estado: true
                        });
                    } else {
                        lines.push({
                            Id: $('#Id').val().trim(),
                            Descripción: row.find('td:eq(0)').html().trim(),
                            Estado: false
                        });
                    }
                });
            }
            if (isAllValid) {
                console.log(lines);
                var data = {
                    Lines: lines
                }
                $.ajax({
                    url: '@Url.Action("SaveUserDepartment", "Logistic")',
                    type: "POST",
                    data: JSON.stringify(data),
                    dataType: "JSON",
                    contentType: "application/json",
                    success: function (d) {
                        if (d.status == "OK") {
                            alert('Datos insertados correctamente');
                        }
                        else {
                            alert(d.status);
                        }
                    }
                });
            }
        }
    </script>
}